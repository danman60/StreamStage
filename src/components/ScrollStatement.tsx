"use client";

import { TextReveal } from "./magicui/text-reveal";

export default function ScrollStatement() {
  return (
    <TextReveal className="font-heading">
      We don't just film performances — we build the technology that powers them.
    </TextReveal>
  );
}
