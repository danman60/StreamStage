<!-- GitNexus rules: see master ~/projects/CLAUDE.md → "GitNexus Workflow" section. Per-project index name is the project folder name. -->

## Auto-Reflections
<!-- Auto-generated by self-reflect. User may edit/delete freely. -->
- When resuming a session after compaction (`sysadmin`, `assistant`), execute a background process audit to identify and kill "ghost" tasks before initiating new workflows.
- Before diagnosing code logic for DART/Tablet failures, verify physical hardware state (lid position, Wi-Fi connection) to distinguish between application bugs and connectivity loss.
- Consolidate domain verification checks into a single robust validation command instead of chaining sequential `ping`, `curl`, and manual analysis steps.
- Ensure the `-home-danman60` baseline workflow always compares git diffs against existing builds (e.g., `footprint-diagram`) to prevent redundant rebuilding before running logic engines.
